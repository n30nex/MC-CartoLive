name: Promote and publish release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  actions: read
  contents: write
  packages: write
  attestations: write
  id-token: write
  security-events: write

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: n30nex/mc-cartolive

jobs:
  promote:
    name: Promote tested digest and publish package
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout tag
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Verify tag, VERSION, and ancestry
        id: release
        run: |
          tag_ref="refs/tags/$GITHUB_REF_NAME"
          test "$(git cat-file -t "$tag_ref")" = "tag"
          merge_sha="$(git rev-parse "$tag_ref^{}")"
          test "$merge_sha" = "$(git rev-parse HEAD)"
          version="$(tr -d '\r\n' < VERSION)"
          test "$GITHUB_REF_NAME" = "v$version"
          git fetch origin main --depth=1
          test "$merge_sha" = "$(git rev-parse origin/main)"
          node scripts/check-version-sync.mjs
          echo "merge_sha=$merge_sha" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "minor=${version%.*}" >> "$GITHUB_OUTPUT"
          echo "build_time=$(git show -s --format=%cI HEAD)" >> "$GITHUB_OUTPUT"
      - name: Require successful main CI for the tagged SHA
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MERGE_SHA: ${{ steps.release.outputs.merge_sha }}
          REPOSITORY: ${{ github.repository }}
        run: |
          runs_json="$(gh api --method GET \
            "repos/$GITHUB_REPOSITORY/actions/workflows/ci.yml/runs" \
            -f head_sha="$MERGE_SHA" -f status=completed -f per_page=100)"
          count="$(printf '%s' "$runs_json" | jq --arg repository "$REPOSITORY" \
            '[.workflow_runs[] | select(.conclusion == "success" and .event == "push" and .head_branch == "main" and .head_repository.full_name == $repository)] | length')"
          test "$count" -gt 0
      - name: Load immutable candidate evidence
        id: evidence
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MERGE_SHA: ${{ steps.release.outputs.merge_sha }}
          VERSION: ${{ steps.release.outputs.version }}
        run: |
          current_main_sha="$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq '.object.sha')"
          test "$current_main_sha" = "$MERGE_SHA"
          artifact_name="release-candidate-$MERGE_SHA"
          artifact_json="$(gh api --method GET \
            "repos/$GITHUB_REPOSITORY/actions/artifacts" \
            -f name="$artifact_name" -f per_page=100)"
          artifact_id="$(printf '%s' "$artifact_json" | jq -r '[.artifacts[] | select(.expired == false)] | sort_by(.created_at) | last | .id // empty')"
          run_id="$(printf '%s' "$artifact_json" | jq -r --argjson id "${artifact_id:-0}" '.artifacts[] | select(.id == $id) | .workflow_run.id')"
          test -n "$artifact_id"
          test -n "$run_id"
          run_json="$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$run_id")"
          printf '%s' "$run_json" | jq -e \
            --arg sha "$MERGE_SHA" --arg repository "$GITHUB_REPOSITORY" \
            '.conclusion == "success" and .path == ".github/workflows/image-candidate.yml" and .event == "workflow_run" and .head_branch == "main" and .head_sha == $sha and .head_repository.full_name == $repository' >/dev/null
          rm -rf /tmp/mc-cartolive-candidate-evidence
          mkdir -p /tmp/mc-cartolive-candidate-evidence
          gh api "repos/$GITHUB_REPOSITORY/actions/artifacts/$artifact_id/zip" > /tmp/mc-cartolive-candidate.zip
          unzip -q /tmp/mc-cartolive-candidate.zip -d /tmp/mc-cartolive-candidate-evidence
          manifest=/tmp/mc-cartolive-candidate-evidence/candidate-manifest.json
          test -f "$manifest"
          printf '%s' "$(cat "$manifest")" | jq -e \
            --arg sha "$MERGE_SHA" --arg version "$VERSION" --arg repository "${REGISTRY}/${IMAGE_NAME}" \
            --arg sourceRepository "$GITHUB_REPOSITORY" --arg candidateRunId "$run_id" \
            '.gitSha == $sha and .sourceCiHeadSha == $sha and .sourceCiConclusion == "success" and .sourceCiWorkflow == "CI" and .sourceCiEvent == "push" and .sourceCiHeadBranch == "main" and .sourceCiHeadRepository == $sourceRepository and .verifiedMainRef == "refs/heads/main" and .verifiedMainSha == $sha and .candidateWorkflowRunId == $candidateRunId and .version == $version and .repository == $repository' >/dev/null
          source_ci_run_id="$(jq -r '.sourceCiRunId' "$manifest")"
          [[ "$source_ci_run_id" =~ ^[0-9]+$ ]]
          source_ci_json="$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$source_ci_run_id")"
          printf '%s' "$source_ci_json" | jq -e --arg sha "$MERGE_SHA" --arg repository "$GITHUB_REPOSITORY" \
            '.name == "CI" and .path == ".github/workflows/ci.yml" and .conclusion == "success" and .event == "push" and .head_branch == "main" and .head_sha == $sha and .head_repository.full_name == $repository' >/dev/null
          digest="$(jq -r '.digest' "$manifest")"
          [[ "$digest" =~ ^sha256:[0-9a-f]{64}$ ]]
          test "$(jq -r '.image' "$manifest")" = "${REGISTRY}/${IMAGE_NAME}@$digest"
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
          echo "run_id=$run_id" >> "$GITHUB_OUTPUT"
      - name: Set up Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
      - name: Log in to GHCR
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Resolve previously built candidate
        id: candidate
        shell: bash
        env:
          MERGE_SHA: ${{ steps.release.outputs.merge_sha }}
          EVIDENCE_DIGEST: ${{ steps.evidence.outputs.digest }}
        run: |
          image="${REGISTRY}/${IMAGE_NAME}"
          candidate="$image:sha-$MERGE_SHA"
          inspect="$(docker buildx imagetools inspect "$candidate")"
          digest="$(printf '%s\n' "$inspect" | awk '$1 == "Digest:" {print $2; exit}')"
          [[ "$digest" =~ ^sha256:[0-9a-f]{64}$ ]]
          test "$digest" = "$EVIDENCE_DIGEST"
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
          echo "image=$image@$digest" >> "$GITHUB_OUTPUT"
      - name: Verify OCI identity on every release platform
        env:
          MERGE_SHA: ${{ steps.release.outputs.merge_sha }}
          VERSION: ${{ steps.release.outputs.version }}
          CANDIDATE_IMAGE: ${{ steps.candidate.outputs.image }}
        run: |
          for platform in linux/amd64 linux/arm64; do
            docker pull --platform "$platform" "$CANDIDATE_IMAGE"
            container_id="$(docker create --platform "$platform" "$CANDIDATE_IMAGE")"
            image_id="$(docker inspect --format '{{.Image}}' "$container_id")"
            revision="$(docker image inspect --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}' "$image_id")"
            source="$(docker image inspect --format '{{ index .Config.Labels "org.opencontainers.image.source" }}' "$image_id")"
            image_version="$(docker image inspect --format '{{ index .Config.Labels "org.opencontainers.image.version" }}' "$image_id")"
            architecture="$(docker image inspect --format '{{.Architecture}}' "$image_id")"
            docker rm "$container_id"
            test "$revision" = "$MERGE_SHA"
            test "$source" = "https://github.com/$GITHUB_REPOSITORY"
            test "$image_version" = "$VERSION"
            test "$architecture" = "${platform#linux/}"
          done
      - name: Verify candidate provenance belongs to this repository
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh attestation verify "oci://${{ steps.candidate.outputs.image }}" --repo "$GITHUB_REPOSITORY"
      - name: Re-smoke exact candidate
        run: |
          node scripts/package-smoke.mjs \
            --image "${{ steps.candidate.outputs.image }}" \
            --version "${{ steps.release.outputs.version }}" --pull
      - name: Re-scan exact candidate
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ steps.candidate.outputs.image }}
          format: sarif
          output: trivy-release.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: "1"
      - name: Upload release scan
        if: always() && hashFiles('trivy-release.sarif') != ''
        uses: github/codeql-action/upload-sarif@99df26d4f13ea111d4ec1a7dddef6063f76b97e9 # v4.37.0
        with:
          sarif_file: trivy-release.sarif
      - name: Promote the same manifest digest
        run: |
          image="${REGISTRY}/${IMAGE_NAME}"
          source="${{ steps.candidate.outputs.image }}"
          docker buildx imagetools create \
            --tag "$image:${{ steps.release.outputs.version }}" \
            --tag "$image:${{ steps.release.outputs.minor }}" \
            --tag "$image:latest" \
            "$source"
          for tag in "${{ steps.release.outputs.version }}" "${{ steps.release.outputs.minor }}" latest; do
            promoted="$(docker buildx imagetools inspect "$image:$tag" | awk '$1 == "Digest:" {print $2; exit}')"
            test "$promoted" = "${{ steps.candidate.outputs.digest }}"
          done
      - name: Generate SPDX SBOM asset
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: ${{ steps.candidate.outputs.image }}
          format: spdx-json
          output-file: /tmp/mc-cartolive-${{ steps.release.outputs.version }}.spdx.json
          upload-artifact: false
          upload-release-assets: false
      - name: Build deployment bundle and release manifest
        env:
          RELEASE_IMAGE: ${{ steps.candidate.outputs.image }}
          RELEASE_GIT_SHA: ${{ steps.release.outputs.merge_sha }}
          RELEASE_BUILD_TIME: ${{ steps.release.outputs.build_time }}
        run: |
          node scripts/build-release-bundle.mjs
          mkdir -p artifacts/release/assets
          tar -C artifacts/release/stage -czf "artifacts/release/assets/mc-cartolive-${{ steps.release.outputs.version }}-deploy.tar.gz" \
            "mc-cartolive-${{ steps.release.outputs.version }}"
          cp artifacts/release/release-manifest.json artifacts/release/assets/
          cp docs/public-api.openapi.json artifacts/release/assets/
          cp trivy-release.sarif artifacts/release/assets/
          cp "/tmp/mc-cartolive-${{ steps.release.outputs.version }}.spdx.json" artifacts/release/assets/
      - name: Generate checksums
        working-directory: artifacts/release/assets
        run: sha256sum * > SHA256SUMS
      - name: Attest release assets
        uses: actions/attest@a1948c3f048ba23858d222213b7c278aabede763 # v4.1.1
        with:
          subject-path: artifacts/release/assets/*
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "$GITHUB_REF_NAME" artifacts/release/assets/* \
            --verify-tag \
            --title "MC-CartoLive ${{ steps.release.outputs.version }}" \
            --notes-file docs/3.2.0/release_notes.md
